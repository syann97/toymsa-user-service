server:
  port: 60000

spring:
  application:
    name: discovery-client

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka
    fetch-registry: true
    register-with-eureka: true

